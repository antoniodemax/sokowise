"""Read-only aggregate queries over the transactional tables (PRD FR-I, ARCHITECTURE §3.2).

Used by the analytics API now and by the AI tools later. Imports repositories and
models only; never services.
"""
